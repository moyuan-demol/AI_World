# ============================================================
# 本文件供 Streamlit Community Cloud 使用（streamlit_app.py）
#
# 说明：Streamlit 与 FastAPI 后端是两套独立的运行环境，故意分开：
#   - 本文件：Streamlit 演示版（不安装 fastapi，避免与 streamlit 的
#     starlette 依赖范围冲突）
#   - backend/requirements.txt：FastAPI 后端（不安装 streamlit）
#
# 本地同时开发两者时，建议各自使用独立虚拟环境。
# ============================================================

# ---- Streamlit 演示版 ----
streamlit>=1.39.0

# starlette 显式固定：streamlit 要求 >=0.46,<2，此版本已随 AppTest 验证
starlette==0.47.3

# ---- 后端 Service 层所需（不含 fastapi）----
sqlalchemy==2.0.36
greenlet==3.1.1
aiosqlite==0.20.0
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.27.2
PyJWT==2.10.1
pypdf==5.1.0
python-docx==1.1.2
