# ============================================================
# 本文件供 Streamlit Community Cloud 使用（streamlit_app.py）
#
# 重要：Streamlit Cloud 的 Python 环境由平台维护，里面已经装好一部分包。
# 这里刻意使用「下限 + 上限」的宽松约束，而不是 == 精确固定 ——
# 精确固定会和平台已有依赖产生 pip 冲突，导致
# "installer returned a non-zero exit code / Error during processing dependencies"。
#
# Streamlit 与 FastAPI 是两套独立运行环境：
#   - 本文件：Streamlit 演示版（不安装 fastapi）
#   - backend/requirements.txt：FastAPI 后端（精确固定，供 Docker / Render 使用）
# ============================================================

streamlit>=1.39,<2

# ---- 后端 Service 层所需（不含 fastapi）----
sqlalchemy>=2.0.30,<3
greenlet>=3.0,<4
# 本地 SQLite
aiosqlite>=0.19,<1
# 云 PostgreSQL（Neon / Supabase 免费档）异步驱动
asyncpg>=0.29,<1
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
httpx>=0.27,<1
PyJWT>=2.8,<3
pypdf>=4.0,<7
python-docx>=1.1,<2
# 更多文件类型：Excel / PPT
openpyxl>=3.1,<4
python-pptx>=1.0,<2
xlrd>=2.0,<3
striprtf>=0.0.26
